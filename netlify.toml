[build]
  functions = "netlify/functions"
  publish = "frontend/build"

[[redirects]]
  from = "/api/*"
  to = "https://enedina-matrilateral-gertude.ngrok-free.dev/:splat"
  status = 200
  force = true
  # preserve_query is automatically handled by Netlify
